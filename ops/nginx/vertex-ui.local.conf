# Combined fallback HTTP config (no TLS). Prefer using:
# - vertex-ui.local.http.conf
# - vertex-ui.local.https.conf
server {
    listen 80;
    server_name vertex-ui.local;
    root /var/www/vertex-react-app/dist;
    index index.html;

    location / {
        try_files $uri /index.html;
    }
}
